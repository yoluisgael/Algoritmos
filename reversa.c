#include <stdio.h>
#include <stdlib.h>

void reverse(char *str){
    char* temp;
    while(*str != '\0'){
        
    }
}

int main (){
    char cadena[20];
    gets(cadena);
    reverse(cadena);
    return 0;
}